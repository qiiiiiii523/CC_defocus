# DGNO-Cell · 3DHistech (cytopathology, RGB)
name: DGNO_3DHistech_Cell
model_type: ImageCleanModel86
scale: 1
num_gpu: 1
manual_seed: 100

# dataset and data loader settings
datasets:
  train:
    name: TrainSet
    type: Dataset_PairedImage
    dataroot_gt: ./datasets/3DHistech/train/sharp
    dataroot_lq: ./datasets/3DHistech/train/blur
    geometric_augs: true

    filename_tmpl: '{}'
    io_backend:
      type: disk

    use_shuffle: true
    num_worker_per_gpu: 8
    batch_size_per_gpu: 8

    ### ------- Progressive training -------
    mini_batch_sizes: [8,5,4,3,2]
    iters: [92000,64000,48000,36000,60000]
    gt_size: 256
    gt_sizes: [128,160,192,224,256]
    ### ------------------------------------

    dataset_enlarge_ratio: 1
    prefetch_mode: ~

  val:
    name: ValSet
    type: Dataset_PairedImage
    dataroot_gt: ./datasets/3DHistech/val/sharp
    dataroot_lq: ./datasets/3DHistech/val/blur
    crop_size: 256
    crop_sizes: [256]
    io_backend:
      type: disk

# network structures
network_g:
  type: DGNO
  variant: cell
  inp_channels: 3
  out_channels: 3
  dim: 48
  num_blocks: [2,4,6,2]
  num_refinement_blocks: 4
  heads: [1,2,4,8]
  ffn_expansion_factor: 2.66
  bias: False
  LayerNorm_type: WithBias

  learnable_tau: True
  init_tau: 0.25
  flux_mode: jump
  boundary_type: neumann      # "neumann", "periodic", "dirichlet"
  win_dg: 8

# path
path:
  pretrain_network_g: ~
  strict_load_g: true
  resume_state: ~

# training settings
train:
  total_iter: 300000
  warmup_iter: -1
  use_grad_clip: true
  ema_decay: 0.999

  scheduler:
    type: CosineAnnealingRestartCyclicLR
    periods: [92000, 208000]
    restart_weights: [1,1]
    eta_mins: [0.0003,0.000001]

  mixing_augs:
    mixup: false
    mixup_beta: 1.2
    use_identity: true

  optim_g:
    type: AdamW
    lr: !!float 3e-4
    weight_decay: !!float 1e-4
    betas: [0.9, 0.999]

  pixel_opt:
    type: L1Loss
    loss_weight: 1
    reduction: mean

# validation settings
val:
  window_size: 8
  val_freq: !!float 4e3
  save_img: false
  rgb2bgr: true
  use_image: false
  max_minibatch: 8

  metrics:
    psnr:
      type: calculate_psnr
      crop_border: 0
      test_y_channel: false

# logging settings
logger:
  print_freq: 1000
  save_checkpoint_freq: !!float 4e3
  use_tb_logger: true
  wandb:
    project: ~
    resume_id: ~

# dist training settings
dist_params:
  backend: nccl
  port: 29500
